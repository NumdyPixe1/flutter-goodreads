import 'package:flutter/material.dart';
import 'package:get/get.dart';

class FeaturedListButtonWidget extends GetView {
  const FeaturedListButtonWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(onPressed: () {}, child: Column(children: []));
  }
}
